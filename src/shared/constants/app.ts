/** Versão do app — injetada pelo Vite a partir do package.json */
export const APP_VERSION = `v${__APP_VERSION__}`

/** Nome da pasta userData / productName no Electron */
export const APP_PRODUCT_NAME = 'Central Adoração'
