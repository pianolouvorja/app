# TASK SPEC: Mutation Testing Hardening — useProjectionStore

## OBJETIVO
Aumentar mutation score do `useProjectionStore` e módulos relacionados de ~87% → **≥80% (LITE não exige B3, mas boa prática)** eliminando mutantes sobreviventes **não-equivalentes**.

## CONTEXTO
- `useProjectionStore`: 13 testes passando, coverage 100%
- Mutation score atual: ~87% (item-helpers 92%, preferences 90%, web-projection 87%)
- Mutantes sobreviventes conhecidos (18/09):
  - Ternários `music.MIN = 0` (equivalentes reais)
  - Regex IP mascarada (equivalentes reais)
  - `0 <= 0` vs `0 < 0` (equivalentes reais)
  - **Não-equivalentes**: branches em `reconcileTargetDisplays`, `pruneReturnDisplay`, `refreshDisplays` error paths

## ESCOPO
Apenas arquivos em `src/modules/settings/stores/` e `services/display-service.ts`

## CRITÉRIOS DE ACEITE
- [ ] **RF-MUT-01**: Mutation score ≥ 80% em `useProjectionStore.test.ts` (stryker)
- [ ] **RF-MUT-02**: Zero mutantes sobreviventes **não-equivalentes** nos arquivos tocados
- [ ] **RF-MUT-03**: Testes existentes continuam passando (regressão zero)
- [ ] **RF-MUT-04**: Cobertura mantida 100%

## ARQUIVOS ALVO
- `src/modules/settings/stores/useProjectionStore.ts`
- `src/modules/settings/stores/__tests__/use-projection-store.test.ts`
- `src/modules/settings/services/display-service.ts`

## ABORDAGEM SUGERIDA
1. Rodar `npx stryker run --mutate "src/modules/settings/stores/**/*.ts" --testRunner vitest`
2. Identificar mutantes sobreviventes **não-equivalentes** (excluir ternários MIN=0, regex IP, 0<=0)
3. Adicionar testes que matam os não-equivalentes (edge cases: error paths, empty arrays, boundary conditions)
4. Verificar que `vitest run --coverage` mantém 100%

## CONTRATO DE SAÍDA
N/A (task de hardening, não exporta API)

## ARMADILHAS
- Não adicionar testes que só matam equivalentes (perda de tempo)
- `refreshDisplays` error path: mock `listSystemDisplays` rejecting
- `reconcileTargetDisplays`: testar arrays vazios, IDs inválidos, duplicados
- `pruneReturnDisplay`: testar return display removido